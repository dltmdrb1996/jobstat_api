package com.example.jobstat.core.constants

object RestUrlConstants {
    object ApplicationName {
        const val LSG = "/lsg"
        const val CONNECTION_ERROR = "Network connection failed"
    }

    object VersionName {
        const val V1 = "/v1"
    }

    object DomainName {
        const val SAMPLE = "/sample"
    }
}
